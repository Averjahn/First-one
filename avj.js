const test = {
    el: '#avj',
    data() {                    // Данные для работы с Vue 
        return {                //Объект
            
        }
    },
    methods: {
        
    }
}

Vue.createApp(test).mount('#avj')